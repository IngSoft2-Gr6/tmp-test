{
  "name": "test-repo",
  "version": "2.0.3",
  "main": "index.js",
  "repository": "https://github.com/IngSoft2-Gr6/tmp-test",
  "author": "SGman98 <bastiangarnica@gmail.com>",
  "license": "UNLICENSED",
  "scripts": {
    "release:rename": "git branch -m $(echo $(git rev-parse --abbrev-ref HEAD) | cut -d '/' -f1)/$npm_package_version",
    "release:start": "git flow release start x.x.x",
    "release:finish": "git flow release finish $npm_package_version",
    "release:bump": "yarn version --no-git-tag-version --no-commit-hooks --no-push",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
    "changelog:latest": "conventional-changelog -p angular -o CHANGELOG.latest.md",
    "release": "run() { yarn release:start && yarn release:bump $@ && yarn release:rename; }; run ",
    "release:full": "run() { yarn release $@ && yarn changelog && yarn changelog:latest && git add package.json CHANGELOG.md CHANGELOG.latest.md && git commit -m \"chore(release): $npm_package_version\" && yarn release:finish; }; run"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^2.2.2"
  }
}
