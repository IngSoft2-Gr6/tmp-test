{
  "name": "test-repo",
  "version": "1.2.6",
  "main": "index.js",
  "repository": "git@github.com:IngSoft2-Gr6/tmp-test.git",
  "author": "SGman98 <bastiangarnica@gmail.com>",
  "license": "UNLICENSED",
  "scripts": {
    "release:rename": "git branch -m $(echo $(git rev-parse --abbrev-ref HEAD) | cut -d '/' -f1)/$npm_package_version",
    "release:start": "git flow release start $npm_package_version",
    "release:finish": "git flow release finish $npm_package_version",
    "release:bump": "yarn version --no-git-tag-version --no-commit-hooks --no-push",
    "release:changelog": "changelog",
    "release": "yarn release:start && yarn bump $1 && yarn release:rename",
    "release:full": "yarn release $1 && changelog && git add package.json CHANGELOG.md && git commit -m \"New version: $npm_package_version\" && yarn release:finish"
  },
  "devDependencies": {
    "generate-changelog": "^1.8.0"
  }
}
